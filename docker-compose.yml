version: '3.1'

services:
  tgbot:
    container_name: bot
    build:
      context: .
    working_dir: "/src/"
    command: python3 app.py
    networks:
      - botnet
    restart: always
    env_file:
      - ".env"
    volumes:
      - .:/src

networks:
  botnet:
    driver: bridge
